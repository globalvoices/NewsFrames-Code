<h1>Sign Up</h1>
<div class="row">
  <div class="col-md-6">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>

      <div class="form-group">
        <%= f.label :name %><br />
        <%= f.text_field :name, autofocus: false, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: false, class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :password %>
        <% if @minimum_password_length %>
        <em>must be at least <%= @minimum_password_length %> characters long</em>
        <% end %><br />
        <%= f.password_field :password, autocomplete: "off", class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.label :password_confirmation %><br />
        <%= f.password_field :password_confirmation, autocomplete: "off", class: 'form-control' %>
      </div>

      <div class="form-group">
        <%= f.submit "Sign up", class: "btn btn-primary" %>
      </div>
    <% end %>
  </div> <!-- /.col -->

  <div class="col-md-6">
    <p>NewsFrames is a web app to help research news stories and frame them. Collaborate with other people to conduct research for a story or create a new media environment.</p>

    <img src="http://placehold.it/600x350" alt="" class="img-responsive">
    <div class="spacing"></div>
  </div> <!-- /.col -->
</div> <!-- /.row -->
